import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  table: {
    minWidth: 300,
  },
  container: {
    padding: '20px 15px',
    marginTop: '30px',
  },
  errorMessage: {
    paddingTop: '10px',
  },
});

export default useStyles;
